using System.Collections;
using System.Collections.Generic;
using RPGCharacterAnims;
using UnityEngine;

public class CharacterInitializer : Singleton<CharacterInitializer>
{
    public Characters activeCharacter = Characters.Jisa; //we will have a way to check later
    
    
    
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
